# V2.5 Production Candidate Freeze — Result

- **Freeze Decision**：**PRODUCTION CANDIDATE FREEZE**
- **Freeze Date**：2026-09-13
- **Commit SHA**：`61eb6a4668c600ed890ce7d1f76fd5bc5a85fa08`（`freeze: v2.5 production candidate`，17 files / +4779 −16）
- **Tag**：`v2.5-production-candidate`（annotated，tag object `5a60928`，指向 freeze commit）
- **Branch**：`main`（base commit `9b22065` → freeze commit `61eb6a4`）
- **Remote**：`origin` → `https://github.com/WongMingJiu/landingpage-test-hero-page`（branch + tag 均已 push 并经 `git ls-remote` 验证）

## Test Result（§10）

```text
command: python3 -m unittest discover -s tests
total:   122
passed:  122
failed:  0
elapsed: 0.096s
```

## Smoke Result（§11/§12）

v03 / v04 / v06 各跑一次完整文本链（V2.1a → V2.1b → V2.3，gpt-5.5/temp=0，
不生图），9 stages 全部 attempt 1 / 0 重试：

```text
pipeline success       3/3
structural hard fail   0/3
compliance hard fail   0
grounding safety fail  0
used_anchors membership 3/3 pass
```

明细（`output/v2.5-freeze/smoke/smoke-summary.json`）：v03 = 77.3s/21.0s/110.9s；
v04 = 156.8s/26.9s/55.3s；v06 = 154.6s/38.3s/56.9s；三次 grounding_status
均为 sufficient，schema/slot/compliance/trace/membership 全空错误。

## Frozen SHA Fingerprints（working tree 现算，2026-09-13）

| 资产 | SHA256-16 |
|---|---|
| V2.1a prompt（creative_tagging.md） | `1129c5ff55976e0c` |
| V2.1b prompt（intent_decision.md） | `6db9eef0b8a5dc50` |
| V2.3 prompt（message_match_copy.md，V1.2 Frozen） | `961cdadfe1e0c6e7` |
| V2.3 schema / validator（message_match_schema.py） | `da9755eee8805752` |
| V2.1a schema（schema.py） | `b2be9d5904c2fa60` |
| V2.1b schema（intent_schema.py） | `ba9e187b3f9014ba` |
| opening taxonomy | `1ad257d0481899c0` |
| user expectation taxonomy | `989d11620e68cd0f` |
| value taxonomy | `438a2d462362cc62` |

与 prompt 已知值比对：V2.1a / V2.1b / V2.3 prompt 与 validator 四项全部一致。

## Production Gate 结论（人工评审已确认，V2.5-F）

```text
V2.1b Route Stable        30/30
V2.3 Same Answer Class    30/30
E2E Material Route Drift  0/30
v04 Structural Hard Fail  0/3
Compliance Hard Fail      0
Grounding Safety Fail     0
```

> Production Candidate Freeze 不是正式线上放量，只代表当前文本链已经通过
> 离线生产候选稳定性验证，可以进入小流量线上实验准备。

## Known Non-Blocking Issues（Observability，随 A/B 进入监控）

| 指标 | 观测值 |
|---|---|
| Opening Type Drift | 3/30 |
| Primary Tag Drift | 18/30 |
| Expectation Drift | 3/30 |
| Anchor Density Drift | 12/30 |
| Supporting Drift | 0/30 |
| Taxonomy Disagreement | 3/30 |

特别记录：v09「低门槛领取型」→ Taxonomy Review Backlog → 不阻塞 Freeze。

## 执行过程记录

1. **Precheck（§3/§4）**：diff 三分类——A 类（3 prompt/validator 修改 + 7
   harness + 5 tests + 2 freeze docs）全部进入 commit；B 类（output/ 等）
   由 .gitignore 排除；C 类（.qoder/、.workbuddy/、.zcode/、docs PRD 草稿）
   不提交、保留为 working tree 残留。无 Unexpected 代码变化。
2. **指纹（§5）**：9 项 SHA256 从 working tree 重新计算，未复制旧报告。
3. **测试 + Smoke（§10-§12）**：122/122 单测过；smoke 3/3 过。
4. **Commit/Tag/Push（§13-§15）**：commit 仅含 A 类 17 文件（密钥扫描干净，
   config.env 由 .gitignore 保护未入库）；annotated tag 指向 freeze commit；
   branch + tag push 成功（代理环境走系统代理直连，无需 no_proxy）。
5. **Push 后验证（§16）**：HEAD=freeze commit、tag 指向 HEAD、
   `git ls-remote` 确认 remote main=61eb6a4 与 tag 存在、working tree
   仅剩 C 类残留（Freeze 资产全部已提交）。

## Next Step

> 下一步进入小流量线上 A/B 实验准备（Small Traffic Online A/B Experiment
> Preparation）。
