# V2.1a Final Acceptance Report（窄口径迭代：salience + v04 边界 + benchmark 一致性 + 全量重跑）

> 范围：仅执行用户授权的 4 项窄改动（A 诊断 / B Primary Salience / C v04 边界 / D GT 审计 / E 评估指标 / F 重跑）。
> 未改架构、未改 taxonomy、未实现 Primary Driver 及任何 V2.1b 特性。
> 最终实现：`v2/` @ 工作区当前状态；最终全量跑批：`output/benchmark-runs/v2.1a-final-b`；稳定性：`output/benchmark-runs/v2.1a-stability`。

---

## 1. 本轮代码 / Prompt 变更

| 变更 | 位置 | 说明 |
|---|---|---|
| B. Primary Salience 一致性校验 | `v2/prompts/creative_tagging.md` §2.3.1 | 三问检查（解释 Opening 成立 / 解释用户在意什么 / 处于 0–30s 核心因果链）；禁止仅按"最清楚/提及最多"晋升；`获得课程与学习资源`、`获得专业指导`、`降低时间与地点限制`、`零基础可学`、`便捷领取并快速开始` 列为默认 supporting、需更强中心性证据才可 Primary（非禁止）；Opening/Expectation 仅为支持信号；v09 反例保留 |
| C. v04 边界规则 | `v2/prompts/creative_tagging.md` §3.2.1 | `后悔/没想到/你知道为什么吗` 等措辞不足以判 `反常识/悬念型`；"人物→学习事实/路径→可见改善→社会证明"结构优先 `学员故事证明型`；仅当信息缺口本身是继续观看主因才用悬念型；不硬编码任何视频 |
| D. GT 审计与修正 | `v2/benchmarks/.../manifest.json` `gt_adjustments` | v08/v09 原 GT 违反 Spec §6.4 分类上限，manifest 已修正并逐条记录理由；canonical markdown 保留为历史 SoT |
| E. 评估指标 | `v2/benchmarks/run_benchmark.py` | 保留 `primary_exact` 严格指标；新增 `primary_core_match ∈ {exact, partial, wrong}`，两者同时上报 |
| A. 诊断工具 | `v2/benchmarks/diagnose_salience.py`（新增） | 对任一 run 目录产出"识别 vs 显著性"诊断表（MISSING / DEMOTED / 错误晋升分类） |
| 修复：超时回退失效 | `v2/tagging.py` | `_is_payload_too_large` 重复定义导致带 timeout 标记的版本被无 timeout 版本覆盖，网关超时不再回退 staged 路径（v02 曾因此 FAILED）；删除冗余定义 |

## 2. 单测 / 回归

`python -m unittest discover tests` → **52 tests OK**（含 GT 约束审计测试：Active ≤5、Primary 1–2、Supporting 0–3、分类上限、v08/v09 修正记录存在性）。

## 3. Benchmark GT 修正（D）

逐条审计 10 条 GT 对照 Spec §6.4：

- v08：原 supporting 含 3 个 `专业认知提升` Active（超限 2）→ 保留 `纠正错误唱歌认知`、`获得演唱效果参照`，移除 `理解发声原理`。
- v09：原 supporting 含 3 个 `学习降阻` Active（超限 2）→ 保留 `零基础可学`、`获得专业指导`，移除 `降低乐理与理解门槛`。
- 其余 8 条 GT 全部满足约束，无调整。审计逻辑已固化为回归测试。

## 4. 最终 10 视频全量重跑（v2.1a-final-b，from scratch，staged 512，temperature=0）

| 指标 | 结果 | 目标 | 判定 |
|---|---:|---:|---|
| JSON parseable | 10/10 | 10/10 | PASS |
| Schema valid | 10/10 | - | PASS |
| Decision Window（不误扩窗） | 10/10 | 10/10 | PASS |
| Opening Type | **9/10** | ≥9 | PASS（v09 错） |
| User Expectation | **9/10** | ≥9 | PASS（v06 错） |
| primary_exact（严格诊断） | **3/10** | diagnostic | v01/v03/v05 |
| primary_core_match | exact 3 / partial 6 / wrong 1 → **aligned 9/10** | ≥8（core） | PASS（v02 wrong） |
| Evidence present | 10/10 | 100% | PASS |

Opening/Expectation 均 ≥9/10 → 按指令触发稳定性测试（§6）。

## 5. A. 识别 vs 显著性诊断（基于 v2.1a-final-b）

对 20 个期望 Primary 标签逐个判定：

| 分类 | 数量 | 明细 |
|---|---:|---|
| OK（primary 命中） | 12 | v01×2, v03×2, v05×2, v06(纠正错误唱歌认知)，v04/v07(效果参照)/v08/v09/v10 各 1 |
| **MISSING（识别失败）** | 3 | v02 `获得演唱效果参照`；v08 `正确发声并保护嗓音`；v10 `正确发声并保护嗓音` |
| **DEMOTED（显著性失败）** | 5 | v02 `获得专业指导`；v04 `获得演唱效果参照`；v06 `正确发声并保护嗓音`；v07 `获得他人认可`；v09 `降低年龄焦虑`(strong 却 supporting) |

错误晋升的 Primary（预测 primary 不在 GT primary 中）：

| id | 错误晋升标签 | 类型 |
|---|---|---|
| v02 | 方法简单易操作、降低时间与地点限制 | 泛化辅助值双双晋升（B 规则的目标失效场景） |
| v06 | 理解发声原理 | GT supporting 过度晋升 |
| v07 | 获得可复制学习范例 | GT supporting 过度晋升 |
| v08 | 理解发声原理 | GT 未入 active |
| v09 | 零基础可学 | GT supporting 过度晋升（年龄焦虑反被降级） |

**结论：Primary 剩余问题以 Salience Ranking 失败为主（5 降级 + 6 错误晋升），Tag Recognition 失败次之（3，且 2 个同为 `正确发声并保护嗓音`）。** 完整表见 `output/benchmark-runs/v2.1a-final-b/salience_diagnostic.md`。

## 6. 稳定性测试（v2.1a-stability，每样本 ×3，共 30 次，全部完成、0 FAILED）

单次运行整体准确率：

| run | Opening | Expectation | primary_exact | core aligned | Window | Evidence | Schema |
|---|---:|---:|---:|---:|---:|---:|---:|
| run0 | 10/10 | 9/10 | 2/10 | 10/10 | 10/10 | 10/10 | 10/10 |
| run1 | 8/10 | 9/10 | 4/10 | 9/10 | 10/10 | 10/10 | 10/10 |
| run2 | 10/10 | 7/10 | 2/10 | 10/10 | 10/10 | 10/10 | 10/10 |

漂移情况：

- **Opening 漂移 3 样本**：v02（教学演示型↔演唱效果型，两者均在 GT 可接受集内）；v04（学员故事证明型↔剧情/内容叙事型）；v09（低门槛领取型↔权威背书型）。
- **Expectation 漂移 3 样本**：v02（掌握简单方法↔低门槛开始学习）；v05（理解专业原理↔获得/复制演唱效果）；v06（掌握简单方法↔理解专业原理）。
- **Active 标签集合漂移 9/10**（集合精确相等少，但 core aligned 每轮 9–10/10，核心语义稳定）。
- Window/Evidence/Schema 三轮全稳 10/10。

**稳定性判定：未完全达标。** Opening/Expectation 各有 3 个边界样本跨轮漂移，主因是 staged 分批笔记+合并路径的自由文本中间层引入非确定性（temperature=0 无法消除）。这属于验收缺口，如实上报，不在本轮窄口径内扩大改动。

## 7. 剩余失败案例

| 案例 | 维度 | 表现 | 性质 |
|---|---|---|---|
| v09 | Opening | 最终跑判 `反常识/悬念型`（GT `低门槛领取型`）；模型以"什么类型的人适合学"的采访提问为信息缺口 | 提问式钩子 vs 低门槛 Offer 的优先级边界；稳定性轮中 2/3 次判对 |
| v10 | Opening | 最终跑判 `演唱效果型`（GT `教学演示型`，师生同唱+方法字幕）；稳定性 3 轮均判对 | 决策优先级决胜不稳定（与 v02 同类边界，v02 最终跑判对） |
| v06 | Expectation | 判 `理解专业原理`（GT `掌握简单方法`）；模型以后腰/发动机机理解释压过动作示范 | §9.1"有动作/跟着做→掌握简单方法"与"只讲机制→理解专业原理"的边界 |
| v02 | Primary | 最终跑 core=wrong：`获得演唱效果参照` 未识别 + 两个泛化辅助值晋升 | 识别失败与显著性失败叠加；三轮中仅 1 轮 wrong |

均为边界判断问题，无结构性/schema/窗口类失败。

## 8. 验收小结

- 通过：JSON 10/10、Schema、Decision Window 10/10、Opening 9/10、Expectation 9/10、Primary core 9/10、Evidence 10/10、52 单测。
- 诊断：primary_exact 3/10（严格指标按预期偏低）；剩余 Primary 误差以 salience ranking 为主、recognition 次之。
- 缺口：稳定性 Opening/Expectation 各有 3 个边界样本漂移；active 集合级漂移普遍但核心语义稳定。
- 未做（按指令）：不进入 V2.1b，不实现 Primary Driver，不改架构/taxonomy。
